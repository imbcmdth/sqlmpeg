# 013 — IR v2 + error codes  (model: sonnet · wave A · branch v2-streams)

Read plans/000b-interfaces-v2.md (§ ir.py, § errors) and RFC-001.

## Deliverables
1. `sqlmpeg/ir.py`: StreamType, ref grammar v2 (`src:<alias>:v:<k>`), updated
   `is_src`/`src_alias` (alias only), new `src_parts`, Node.outputs,
   Output dataclass, Graph.outputs (replacing output). to_dict/from_dict
   round-trip. Module docstring documents the v2 ref grammar.
2. `sqlmpeg/errors.py`: add STREAM_NOT_FOUND, INPUT_NOT_FOUND,
   BROADCAST_MISMATCH. Comment SINGLE_OUTPUT_ONLY as retired-in-v2.
3. Rewrite `tests/test_ir.py` for v2 (round-trip, src_parts, helpers).

## Verify
ruff + mypy --strict on both modules; pytest tests/test_ir.py green.
EXPECTED RED (do not fix): test_split, test_emit, test_lower, test_golden,
test_stdlib, test_cli, test_prompt — later plans own those. No git commands.
